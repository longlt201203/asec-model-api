import { FactorModel } from "@db/models";
import { Injectable } from "@nestjs/common";

@Injectable()
export class FactorService {
	async test() {
		// const document = await FactorModel.create({
		//     name: "test",
		// });
		// console.log(document.id)
	}
}
